#!/usr/bin/env ruby
# frozen_string_literal: true

require "irb"
require_relative "../lib/ruby_claude"

# Start an IRB session with the gem loaded, e.g.:
#   RubyClaude.query("hello")
IRB.start(__FILE__)
